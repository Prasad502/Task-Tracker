# example backend env
# Port the backend should listen on
PORT=4000

# Optional: specify a custom data directory (relative or absolute)
# DATA_DIR=./data

# When sprint periods start (YYYY-MM-DD)
SPRINT_EPOCH=2025-01-01
# Length of a sprint in days
SPRINT_LENGTH=14
